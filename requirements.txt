# Core runtime dependencies (grows as components are added).
# torch + transformers come from the conda env locally (CUDA) and CPU wheels on the HF Space.
numpy>=1.26
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
prometheus-client>=0.20
psutil>=5.9
